{
  "name": "dashboardjh",
  "version": "0.1.0",
  "private": true,
  "description": "Single-tenant n8n message dashboard",
  "scripts": {
    "install:all": "cd server && npm install && cd ../client && npm install",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "build": "cd client && npm install && npm run build && cd ../server && npm install && npx prisma generate",
    "start": "cd server && npx prisma db push --accept-data-loss && node src/seed/seedOwner.js && node src/index.js"
  }
}
